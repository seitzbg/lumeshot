- Add a compact About window, available from the app menu and Settings, with project links and offline open source credits.
- Give uploader names room to wrap and replace the cramped Add uploader menu with clearer service descriptions.
- Show upload progress and results in the menu bar and History, with an action to copy the latest upload link.
- Add a Test action that uploads a generated image using a saved uploader, with optional deletion afterward.
- Improve History with larger thumbnails, image/video/failed-upload filters, Space-bar previews, and image and link copying.
- Retry failed uploads or send saved captures to another destination. Keep existing upload links and avoid overwriting newer clipboard contents.
- Separate removing a History entry from deleting a remote upload; both actions keep the local file. Fix Picsur and Imgur deletion to use their authenticated APIs and confirm success.

Existing capture history is upgraded automatically, and saved uploader settings are retained.
